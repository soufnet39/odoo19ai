<odoo>
    <data noupdate="1">

        <!-- Make "Apps" visible only to Administration & Settings groups -->
        <record id="base.menu_administration" model="ir.ui.menu">
            <field name="group_ids" eval="[(6, 0, [
                ref('base.group_system'),
            ])]"/>
        </record>

        <!-- Or, if you want to *add* to existing groups instead of replacing -->
        <!-- <record id="base.menu_administration" model="ir.ui.menu">
            <field name="group_ids" eval="[(4, ref('base.group_user'))]"/>
        </record> -->

    </data>
</odoo>